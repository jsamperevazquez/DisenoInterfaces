def comprobarCaracterCadena(texto):
    d = dict()
    palabras = texto.split()
    for i in range(len(texto)):
        if texto[i] != ' ':
            if not texto[i] in d:
                for palabra in palabras:
                    pass







